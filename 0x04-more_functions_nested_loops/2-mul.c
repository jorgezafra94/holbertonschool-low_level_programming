#include "holberton.h"
/**
 * mul - return de value of multiplication between a and b
 * @a: input one
 * @b: input two
 * Return: the respective value of multiplication
 */
int mul(int a, int b)
{
	long int  mult;

	mult = a * b;

	return (mult);
}
